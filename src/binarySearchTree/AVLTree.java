package binarySearchTree;

/**
 * A Binary search tree that meets a property in which each node has a balance factor of -1, 0 or 1; and thus, each of its searching and adding operations are always made in a O(logn) time, where n is the number of nodes in the tree.
 * @author Jhon Edward Mora - Universidad ICESI - A00355710
 * @param <K> A Comparable object that defines the order property of the nodes.
 * @param <T> The objects to be stored in this data structure.
 */
public class AVLTree<K extends Comparable<K>, T> extends BinarySearchTree<K, T> {

}
